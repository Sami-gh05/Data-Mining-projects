Put `dallas.png` (the Dallas map overlay used by notebook 01) in this folder.
The notebook reads it with `mpimg.imread("images/dallas.png")` and draws it behind the
geospatial scatter plot using extent `[-97, -96.5, 32.55, 33.05]`.
